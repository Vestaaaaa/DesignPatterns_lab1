/*import ShapeValidator from "../src/ShapeValidator.js";

describe("ShapeValidator", () => {
  test("validates correct point data", () => {
    expect(ShapeValidator.validatePointData("10.5 -20")).toEqual([10.5, -20]);
  });

  test("throws on invalid format", () => {
    expect(() => ShapeValidator.validatePointData("abc def")).toThrow();
  });
});
*/

test("example test", () => {
  expect(true).toBe(true);
});
